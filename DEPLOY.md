# Deploy — CronoCapilar

**EN** — The app is ready for deployment (e.g. [Vercel](https://vercel.com)). Push to GitHub, connect the repo to Vercel, and deploy. See the [README](README.md) for scripts (`npm run build`, `npm run start`).

**PT-BR** — A aplicação está pronta para deploy (ex.: [Vercel](https://vercel.com)). Envie o código para o GitHub, conecte o repositório à Vercel e faça o deploy. Consulte o [README](README.pt-BR.md) para os scripts (`npm run build`, `npm run start`).

**ES** — La aplicación está lista para deploy (ej. [Vercel](https://vercel.com)). Sube el código a GitHub, conecta el repositorio a Vercel y haz el deploy. Consulta el [README](README.es-ES.md) para los scripts (`npm run build`, `npm run start`).
